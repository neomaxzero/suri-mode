interface:
  display_name: "Make Operations Idempotent"
  short_description: "Handle retries and partial execution safely"
  default_prompt: "Use $principle-make-operations-idempotent to check this operation for duplicate effects and safe recovery after interruption."
